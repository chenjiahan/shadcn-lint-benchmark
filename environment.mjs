import fs from 'node:fs';import os from 'node:os';import {execFileSync} from 'node:child_process';
const read=p=>{try{return fs.readFileSync(p,'utf8').trim()}catch{return null}};
const cmd=(c,args)=>{try{return execFileSync(c,args,{encoding:'utf8'}).trim()}catch{return null}};
const pkg=JSON.parse(read('package.json'));
const env={timestamp:new Date().toISOString(),platform:os.platform(),release:os.release(),arch:os.arch(),cpuModel:os.cpus()[0]?.model,logicalCpus:os.cpus().length,availableParallelism:os.availableParallelism(),totalMemoryBytes:os.totalmem(),node:process.version,osRelease:read('/etc/os-release'),cpuQuota:read('/sys/fs/cgroup/cpu.max'),memoryLimit:read('/sys/fs/cgroup/memory.max'),runnerImage:process.env.ImageOS,runnerImageVersion:process.env.ImageVersion,runnerArch:process.env.RUNNER_ARCH,commit:cmd('git',['rev-parse','HEAD']),runUrl:process.env.GITHUB_RUN_ID?`${process.env.GITHUB_SERVER_URL}/${process.env.GITHUB_REPOSITORY}/actions/runs/${process.env.GITHUB_RUN_ID}`:null,runAttempt:process.env.GITHUB_RUN_ATTEMPT,dependencies:Object.fromEntries(Object.keys(pkg.dependencies).map(k=>[k,JSON.parse(read(`node_modules/${k}/package.json`)).version])),workerPolicy:'Tool defaults; no CLI overrides; GOMAXPROCS and RAYON_NUM_THREADS unset for child processes',compileCache:'NODE_DISABLE_COMPILE_CACHE=1',lscpu:cmd('lscpu',[])};
fs.writeFileSync('environment.json',JSON.stringify(env,null,2)+'\n');console.log(JSON.stringify(env,null,2));
